{
  "name": "react-flask-monorepo",
  "private": true,
  "version": "1.0.0",
  "engines": { "node": "20.x" },
  "scripts": {
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm --prefix client install && npm --prefix client run build"
  }
}
